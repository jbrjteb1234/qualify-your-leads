# vendor/

/kit shared components, vendored here so this app builds and deploys
standalone. The canonical source lives at `/kit/ts` in the private monorepo —
do not edit these copies directly; change the canonical kit and re-sync
(see DEPLOY.md).
